# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Used to bundle Python applications into stand-alone executables
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or even
#   fail to install.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock

# PEP 582; used by e.g. github.com/lincolnloop/python-extern
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.cache/
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains IDEs: .idea, .iml, .iws, .jps, .dat, .js, .php, .xml, .sql, .yml, .yaml, .json, .properties, .txt, .md, .html, .css, .js, .py, .java, .c, .cpp, .h, .hpp, .cs, .swift, .m, .mm, .go, .rs, .sh, .bat, .ps1, .rb, .pl, .pm, .t, .r, .lua, .sql, .env, .gitignore, .dockerignore, .dockerfile, .npmrc, .yarnrc, .lock, .log, .zip, .tar, .gz, .bz2, .xz, .7z, .dmg, .iso, .exe, .msi, .apk, .ipa, .app, .deb, .rpm, .pdf, .docx, .xlsx, .pptx, .jpg, .jpeg, .png, .gif, .bmp, .svg, .webp, .mp3, .wav, .flac, .mp4, .mov, .avi, .mkv, .flv, .webm, .ttf, .otf, .woff, .woff2, .eot, .sqlite, .db, .dump, .sql, .bak, .tmp, .temp, .swp, .swo, .swn, .DS_Store
.idea/

# PawPulse specific
backend/*.log
backend/venv/
backend/.env
backend/__pycache__/
backend/app/__pycache__/
backend/pawpulse_ml/__pycache__/
